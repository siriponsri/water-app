# คู่มือผู้ดูแลระบบ — ANF3 Laboratory Records

คู่มือนี้ครอบคลุม **การติดตั้ง เชื่อมต่อ ใช้งานประจำวัน และแก้ปัญหา**
ถ้าต้องการรายละเอียดการติดตั้ง Apps Script ทีละไฟล์ (ไฟล์ไหนวางในสเปรดชีตไหน)
ให้อ่าน [`OWNER_DEPLOYMENT.md`](OWNER_DEPLOYMENT.md) ซึ่งมีตาราง **copy ไฟล์ไหนไปวางที่สเปรดชีตไหน** ครบทั้ง 6 คู่

---

## 0. ระบบนี้ทำงานอย่างไร (อ่านก่อน 2 นาที)

```
    คนกรอกงานที่นี่                    เว็บแอปอ่านจากที่นี่
┌────────────────────┐            ┌──────────────────────┐
│  Google Sheets     │ Apps Script│  RPP2 System DB      │
│  (air-test,        │ ──────────▶│  (RPP2-air-record,   │
│   water-r,         │  ซิงก์      │   RPP2-water-record, │
│   Testing)         │            │   RPP2-cv-record)    │
└────────────────────┘            └──────────┬───────────┘
                                             │ อ่านอย่างเดียว (public read /exec)
                                             ▼
                                  ┌──────────────────────┐
                                  │  เว็บแอปในเบราว์เซอร์  │
                                  └──────────┬───────────┘
                                             │ ส่งแค่ "ชื่อเส้นทาง" ไม่ส่งเทมเพลต
                                             ▼
                                  ┌──────────────────────┐
                                  │ Flask บนเครื่องคุณ    │
                                  │ localhost:8000       │
                                  │ .docx ที่ควบคุม → PDF │
                                  └──────────────────────┘
```

**สามข้อที่ต้องจำ**

1. **เว็บแอปไม่เคยเขียนบันทึก** การสร้าง การให้เลข และการซิงก์ เกิดใน Google Sheets เท่านั้น
2. **เทมเพลตเลือกที่เครื่อง ไม่ใช่ที่เบราว์เซอร์** เบราว์เซอร์ส่งแค่ชื่อเส้นทาง เช่น `pw-prw` แล้ว Flask เป็นคนตัดสินว่าใช้ไฟล์ Word ไหน และตรวจว่า CV เลือกวิธีถูกหรือไม่
3. **System Web App ไม่มี shared sync token** จึงต้องตั้ง `Who has access` ให้แคบที่สุดตามนโยบาย Google Workspace

---

## 1. ติดตั้งครั้งแรกบนเครื่อง Windows

ทำตามลำดับ ดับเบิลคลิกทีละไฟล์ ไม่ต้องใช้สิทธิ์ Administrator

| ลำดับ | ไฟล์ | ทำอะไร | ทำกี่ครั้ง |
|---|---|---|---|
| 1 | `INSTALL.bat` | ดาวน์โหลด `uv` สร้าง `.venv` ติดตั้ง Flask | ครั้งเดียว |
| 2 | `INSTALL-MSOFFICE-SUPPORT.bat` | เปิดการแปลง Word → PDF ผ่าน MS Word | ครั้งเดียว |
| 3 | `START-ANF3.bat` | เปิดเซิร์ฟเวอร์แล้วเปิดเบราว์เซอร์ให้ | ทุกครั้งที่ใช้งาน |

`START-ANF3.bat` จะเช็คก่อนว่ามีเซิร์ฟเวอร์รันอยู่แล้วหรือยัง ถ้ามีก็เปิดเบราว์เซอร์เฉยๆ
ไม่เปิดซ้ำจนพอร์ตชน ถ้าอยากเห็นหน้าต่างเซิร์ฟเวอร์ ให้ใช้ `START-SERVER.bat` แทน

**ต้องมี MS Word ติดตั้งบนเครื่อง** ถึงจะสร้าง PDF ได้ — ตัวแปลงใช้ Word ของจริง
เพื่อให้หน้าตาเอกสารตรงกับเทมเพลตที่ควบคุมไว้ทุกจุด

---

## 2. เชื่อมเว็บแอปเข้ากับ System DB ⚠️ ขั้นตอนที่ขาดไม่ได้

นี่คือขั้นตอนที่เคยพลาดมาก่อน ถ้าข้ามไป เว็บจะเปิดได้ปกติแต่ทุกแท็บจะขึ้นว่า
**"System DB URL is not configured"** และไม่มีบันทึกโผล่มาเลย

### 2.1 เอา URL มาจากไหน

ในสเปรดชีต System DB แต่ละตัว (RPP2-air-record, RPP2-water-record, RPP2-cv-record)
ให้ Deploy Apps Script เป็น **Web app** แล้วคัดลอก URL ที่ลงท้ายด้วย `/exec`
วิธี deploy ละเอียดอยู่ใน `OWNER_SETUP_TH.md` ส่วนที่ 3

### 2.2 ใส่ลงไฟล์ `.env.production`

เปิดไฟล์ `.env.production` ที่โฟลเดอร์หลัก ใส่ให้ครบทั้งสามบรรทัด

```
VITE_WATER_READ_URL=https://script.google.com/macros/s/xxxxx/exec
VITE_AIR_READ_URL=https://script.google.com/macros/s/xxxxx/exec
VITE_CV_READ_URL=https://script.google.com/macros/s/xxxxx/exec
```

> **สถานะปัจจุบันในชุดที่ส่งมา:** Water และ Air ใส่ไว้แล้ว
> **`VITE_CV_READ_URL` ยังว่าง** — คุณต้องใส่เอง มิฉะนั้นแท็บ Cleaning Validation
> จะไม่มีบันทึกขึ้นเลย (ปุ่มสร้าง PDF ยังทำงานได้ เพราะไม่เกี่ยวกับ System DB)

### 2.3 สร้างไฟล์ใหม่หลังแก้ URL — สำคัญ

URL พวกนี้ถูก **ฝังตอน build** ไม่ได้อ่านตอนเปิดเว็บ แก้ไฟล์เฉยๆ ไม่พอ ต้องสั่ง build ใหม่

```
BUILD-DIST.bat
```

หรือถ้าใช้ command line: `pnpm build`

### 2.4 ตรวจว่าเชื่อมสำเร็จจริง

```
node validation/validate_wiring.mjs --built
```

ต้องขึ้นว่า `Wiring OK — 3/3 domains configured`
ถ้ายังเห็น `note VITE_..._READ_URL is empty` แปลว่ายังใส่ไม่ครบ
ถ้าขึ้นว่า `is set in .env.production but is NOT in the built bundle` แปลว่าลืม build ใหม่

---

## 3. ใช้งานประจำวัน

1. ดับเบิลคลิก `START-ANF3.bat` — **ปล่อยหน้าต่างสีดำเปิดค้างไว้** ปิดเมื่อไหร่แอปหยุดทันที
2. เบราว์เซอร์เปิดที่ `http://localhost:8000`
3. หน้าแรกคือ **ชั้นวางแฟ้ม** คลิกป้ายอาคารเพื่อสลับชั้น คลิกแฟ้มเพื่อเปิด
   (หรือกดลูกศร ← → เดินไปตามชั้น)
4. ในแฟ้ม: ค้นหา เลือกใบงาน ดูข้อมูล กด **Preview PDF** แล้ว **Print** หรือ **Download**
5. กด `⌘K` / `Ctrl+K` เพื่อกระโดดไปแฟ้มหรือหน้าไหนก็ได้

### พิมพ์หลายใบพร้อมกัน

ในหน้ารายการใบงาน แต่ละแถวมี **ช่องติ๊ก** ติ๊กเลือกกี่ใบก็ได้ (หรือติ๊กหัวตารางเพื่อเลือกทั้งหมด)
แล้วกด **Print N**

ระบบจะสร้าง PDF ทีละใบจากเทมเพลตที่ควบคุมตัวเดิม แล้ว**รวมเป็นไฟล์เดียว** —
กดพิมพ์ครั้งเดียว ได้กระดาษเรียงตามลำดับในรายการ หรือกด Download เก็บเป็นไฟล์เดียวก็ได้

ถ้ามีใบไหนสร้างไม่ได้ (เช่น CV rinse ที่ยังไม่ได้เลือกวิธี) ระบบจะ**ข้ามใบนั้นและบอกเหตุผล**
ใบที่เหลือยังพิมพ์ได้ตามปกติ ไม่เสียทั้งชุด

### เปลี่ยนสีแฟ้มเอง

หน้าชั้นวางมีปุ่ม **Colours** มุมขวาบน เลือกสีของแต่ละอาคารได้อิสระ

- เก็บใน**เบราว์เซอร์เครื่องนี้เท่านั้น** ไม่กระทบเพื่อนร่วมงาน ไม่ต้อง build ใหม่
- ระบบจะเตือนถ้าสีที่เลือก**จางเกินไปจนอ่านไม่ออก** หรือ**ใกล้กับอาคารอื่นเกินไป**
  (ต่างกันน้อยกว่า 22° = บนชั้นจะดูเหมือนแฟ้มใบเดียวกัน)
- ปุ่ม Colours จะมีจุดเล็กๆ เตือนว่าตอนนี้ใช้สีที่ตั้งเอง **ไม่ตรงกับแฟ้มจริงบนชั้นแล้ว**
- กด **Back to the photographed colours** เพื่อคืนค่าเดิมเมื่อไหร่ก็ได้

> สีแฟ้ม = อาคาร ถ้าเปลี่ยนแล้วไปคุยกับคนอื่นว่า "แฟ้มเขียว" อาจคนละใบกัน
> ให้เรียกชื่ออาคารแทนสีจะปลอดภัยกว่า

### แฟ้มบนชั้น — สีคืออาคาร

| สี | อาคาร | แฟ้ม |
|---|---|---|
| ฟ้า | Building 10 | PRW & PW · Air Sampling · CA · Cleaning Validation |
| ม่วงอ่อน | Building 12 | PRW & PW · Air Sampling · CA · Cleaning Validation |
| เขียวมิ้นต์ | Building 16 | PRW & PW · WFI · Air Sampling · CA & Nitrogen · Cleaning Validation |
| ส้ม | Other Locations | **1 แฟ้ม** — Building 11 และ Building 19 อยู่ข้างใน |
| ชมพู | สำรอง | **1 แฟ้ม** สันพิมพ์ `COMING SOON` เปิดไม่ได้ |

รวม **เปิดได้ 14 แฟ้ม อยู่บนชั้น 15 แฟ้ม**

### Cleaning Validation — เลือกวิธีข้างในแฟ้ม

แต่ละอาคารมีแฟ้ม CV ใบเดียว เลือกวิธีหลังเปิดแฟ้ม

```
เปิดแฟ้ม CV
   ├── Contact Plate ──────────────────────▶ cv-contact-template.docx
   └── Rinse ── เลือกวิธีก่อน
          ├── Pour Plate ─────────────────▶ pw-prw-template.docx
          └── Membrane Filtration ────────▶ wfi-pus-template.docx
```

ถ้าเลือกวิธีไม่ตรงกับเส้นทาง เซิร์ฟเวอร์จะปฏิเสธและตอบว่า
*"CV Rinse test method does not match the selected route"* — เป็นการป้องกันโดยตั้งใจ

**บันทึก CV rinse เป็นบันทึก CV ไม่ใช่บันทึก Water** ไม่เข้าลำดับเลขใบงานของ Water

### แท็บเชื่อมต่อระบบอื่น

| แท็บ | ปลายทาง |
|---|---|
| Stock DB | Google Sheet `Lab_Stock` |
| Stock Web | เว็บแอป Lab Stock |
| จองเลขเอกสาร | Google Sheet `Log Documents` |
| Upload Picture | โฟลเดอร์ Google Drive ของแล็บ |
| COA App | `http://192.168.1.10:8000` |

**COA App ใช้ได้เฉพาะตอนอยู่ในวง Wi-Fi `ANF3`** ถ้าอยู่นอกวง แอปจะขึ้นกล่องข้อความ
บอกให้ต่อ Wi-Fi `ANF3` ก่อน แทนที่จะค้างรอเงียบๆ

---

## 4. แก้ปัญหา

| อาการที่เห็น | สาเหตุ | วิธีแก้ |
|---|---|---|
| `No pyvenv.cfg file` แล้วหน้าต่างปิด | โฟลเดอร์ `.venv` ถูกคัดลอก ย้าย หรือ sync มาจากที่อื่น | รัน `START-SERVER.bat` อีกครั้ง — เวอร์ชันนี้ตรวจเจอและซ่อมเอง ถ้ายังไม่หาย ให้ลบโฟลเดอร์ `.venv` แล้วรัน `INSTALL.bat` |
| ทุกแท็บขึ้น `System DB URL is not configured` | ยังไม่ทำข้อ 2 หรือแก้ `.env.production` แล้วไม่ได้ build ใหม่ | ใส่ URL ให้ครบ แล้วรัน `BUILD-DIST.bat` |
| แท็บ Cleaning Validation ว่าง แต่ Water/Air มีข้อมูล | `VITE_CV_READ_URL` ยังว่าง | ใส่ URL ของ RPP2-cv-record แล้ว build ใหม่ |
| `No records` แต่ตั้งค่า URL ครบแล้ว | เครื่องออกเน็ตไม่ได้ หรือ Web app ตั้งสิทธิ์เป็น private | ตั้ง Deploy ให้ **Anyone** เข้าถึงได้ (อ่านอย่างเดียว) และเช็คว่าเปิด URL `/exec` ในเบราว์เซอร์แล้วได้ JSON |
| เซิร์ฟเวอร์หยุดพร้อมโค้ดไม่ใช่ 0 | พอร์ต 8000 ถูกใช้อยู่ | ปิดหน้าต่าง ANF3 อันเก่าก่อน แล้วเปิดใหม่ |
| กด Preview PDF แล้วขึ้น `PDF template is not installed` | ไฟล์ใน `templates/` หาย | คืนไฟล์ `.docx` ทั้ง 5 ตัวกลับเข้าโฟลเดอร์ `templates/` |
| PDF สร้างไม่ได้เลย | ยังไม่ได้รัน `INSTALL-MSOFFICE-SUPPORT.bat` หรือเครื่องไม่มี MS Word | ติดตั้ง MS Word แล้วรันไฟล์นั้น |
| COA App เปิดไม่ขึ้น | อยู่นอกวง Wi-Fi `ANF3` | ต่อ Wi-Fi `ANF3` ก่อน |
| Console ขึ้น `Access-Control-Allow-Origin ... wildcard '*'` แล้ว `net::ERR_FAILED` | ใช้ build เก่าก่อน v7.1g | รัน `BUILD-DIST.bat` ใหม่ |
| แฟ้ม CV ของอาคารว่าง ทั้งที่ซิงก์แล้ว | ซิงก์ด้วยสคริปต์ก่อน v7.1g ที่เขียน building เป็น `10` แทน `Building 10` | วาง `05-cv-user/Code.gs` ใหม่ทับ แล้วซิงก์ใหม่ ดู `OWNER_DEPLOYMENT.md` |

### ตรวจสุขภาพระบบเร็วๆ

เปิดเบราว์เซอร์ไปที่ URL เหล่านี้ ต้องได้ JSON ไม่ใช่หน้า error

- `http://localhost:8000/api/status` — เซิร์ฟเวอร์ยังอยู่ไหม
- `http://localhost:8000/api/pdf-capabilities` — แปลง Word → PDF ได้ไหม

---

## 5. สำรองข้อมูลและย้อนกลับ

**ก่อนเปลี่ยน Apps Script ทุกครั้ง** ให้ทำสำเนาสเปรดชีตนั้นก่อน
(File → Make a copy) ตั้งชื่อลงท้ายด้วยวันที่ เช่น `RPP2-air-record-20260903`

| ต้องสำรอง | อยู่ที่ไหน |
|---|---|
| สเปรดชีตทั้ง 6 ตัว | Google Drive |
| `.env.production` | โฟลเดอร์หลัก — เก็บสำเนาไว้นอกเครื่อง |
| `templates/*.docx` | เอกสารที่ควบคุม ห้ามแก้เอง |

**ย้อนกลับ:** เปิดสเปรดชีต → Extensions → Apps Script → File → See version history
แล้วเลือกเวอร์ชันก่อนหน้า จากนั้น Deploy ใหม่

---

## 6. สิ่งที่ระบบนี้ตั้งใจ **ไม่** ทำ

- **ไม่แก้ไขบันทึก** เว็บแอปอ่านอย่างเดียวโดยการออกแบบ ไม่ใช่เพราะยังทำไม่เสร็จ
- **ไม่ให้เลขใบงาน** เลขมาจาก Apps Script ในสเปรดชีตของเจ้าของระบบ
- **ไม่ใช่เครื่องมืออนุมัติ** เกมฝึกทั้งสามเป็นสถานการณ์สมมติ ไม่บันทึกข้อมูลที่ควบคุม
  และผลที่ได้ไม่ใช่การปล่อยผ่านหรือการยืนยันเชื้อ
- **ไม่เก็บข้อมูลบันทึกถาวรในเบราว์เซอร์** มีแค่แคชสำหรับอ่านออฟไลน์ (`anf3-read-cache-v1`)

---

## 7. คำสั่งตรวจสอบ (สำหรับคนที่แก้โค้ด)

รันให้ผ่านทั้งหมดก่อนส่งมอบทุกครั้ง

```
pnpm check && pnpm test && pnpm build
node validation/validate_wiring.mjs --built
node validation/contrast.mjs
node validation/test_apps_script_security.mjs
node validation/test_cv_contract.mjs
node validation/test_games.mjs
node validation/test_worksheet_numbering.mjs
node validation/validate_non_game_contract.mjs
python3 validation/validate_cv_package.py
python3 validation/validate_release.py
```

รายละเอียดว่าแต่ละตัวคุ้มครองอะไร อยู่ใน [`HANDOFF.md`](HANDOFF.md)

---

## 8. ไม่แน่ใจให้ถามใคร

| เรื่อง | คนที่ตัดสิน |
|---|---|
| เนื้อหาในเทมเพลต Word ข้อความบนฉลากที่ควบคุม | เจ้าของเอกสาร / QA |
| สิทธิ์เข้าถึงสเปรดชีต การ deploy | เจ้าของระบบ |
| เลขใบงานและลำดับเอกสาร | ผู้ดูแลเลขเอกสาร |
| หน้าตาเว็บ การทำงานของแอป | ผู้ดูแลโค้ด (`HANDOFF.md`) |

**อย่าเดาข้อความบนฉลากที่ควบคุม** ถ้าไม่มีต้นฉบับให้ถาม อย่าแต่งขึ้นเอง
