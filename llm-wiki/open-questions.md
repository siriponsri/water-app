# Open Questions

## Needs User Or Evidence

- What is the final CV System DB Web App `/exec` URL? It is stored in Apps Script Properties and is not present in source.
- Have Air and Water System Web Apps been redeployed as New Versions? On 2026-09-01 their live URLs passed `ping` but returned `Unknown action` for the new `search` contract.
- Has the target deployment PC completed Word smoke tests for the owner-approved `pw-prw-template.docx` and `wfi-pus-template.docx` CV Rinse routes?
- Which Growth Promotion training fixtures and acceptance criteria have Microbiology/QA approval? Current fixtures are explicitly synthetic.
- Has an approved real record been used for a final Microsoft Word PDF visual comparison on the deployment PC?
- Live MCP/clasp resources were not exposed to this session and `clasp` CLI was absent, so live sheet/schema verification still needs an owner run or a future connected session.
