# Archived implementation brief

This file is retained as a historical handoff only. It is not an active
instruction set and must not override `AGENTS.md`, `docs/GOAL.md`, or
`docs/TASKS.md`.

The current application is the supported React frontend plus the local Flask
document service. The current release path is the share-drive launcher
`START-ANF3.bat`. Owner-only plans, Apps Script deployment details, controlled
DOCX templates, and live endpoint checks belong in the external controlled
release package and must not be recreated in the public repository.
